"use client";

import styled from "@emotion/styled";

export const Form = styled.form`
  display: grid;
  gap: ${(p) => p.theme.spacer.xs};
`;

export const ButtonPlace = styled.div`
  text-align: right;
`;
